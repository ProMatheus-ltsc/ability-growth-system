# 本地开发/部署的环境变量（此文件已被 gitignore，不会提交）
# 复制为 .dev.vars 并按需填写：
#   D1_DATABASE_ID=<wrangler d1 create 输出的 database_id>
#   SYNC_AUTH_TOKEN=可选，与 wrangler secret put SYNC_AUTH_TOKEN 设置的值一致时才有意义
D1_DATABASE_ID=
