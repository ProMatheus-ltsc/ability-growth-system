/**
 * 问题跟进 · 日历视图独立路由 - 复用 PdcaCalendarBoard 组件
 */
import { PdcaCalendarBoard } from '../components/PdcaCalendarBoard';

export function PdcaCalendarPage() {
  return <PdcaCalendarBoard fullPage />;
}
